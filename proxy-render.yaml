services:
  - type: web
    name: poke-mcp-proxy
    runtime: python
    plan: free
    buildCommand: pip install -r proxy-requirements.txt
    startCommand: python poke_proxy.py
    envVars:
      - key: PORT
        value: 10000
      - key: PROXY_PORT
        fromService: true